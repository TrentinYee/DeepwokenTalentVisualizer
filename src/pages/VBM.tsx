import Button from "../components/Button";

const VBM = () => {
  return (
    <Button color="primary" onClick={() => console.log("Clicked")}>
      Charisma
    </Button>
  );
};

export default VBM;
